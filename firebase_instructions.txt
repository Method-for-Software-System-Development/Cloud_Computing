📘 HOW TO USE FireBase.py (Firebase Interface)

1. Clone the repository:
   git clone https://github.com/YOUR_USERNAME/Cloud_Computing.git

2. In your Python code or Google Colab, add the Firebase module path:

   import sys
   sys.path.append('/content/Cloud_Computing/firebase')  # For Colab
   # Or the full local path on your machine

3. Import Firebase functions:
   from FireBase import add_user, get_user, update_user, delete_user, add_multiple_users

4. Use the functions normally:

   status, data = get_user("matan_tal")
   print(data)

   new_user = {
       "First name": "Lior",
       "Last name": "Levi",
       "Email": "lior@example.com",
       "Role": "Developer",
       "score": 90
   }

   status, result = add_user("lior_levi", new_user)

5. Notes:
   - Make sure FireBase.py is located in /firebase inside the repo
   - Do not forget to add `.json` when interacting with Firebase (already handled in functions)
   - All team notebooks should be placed inside the /notebooks directory
